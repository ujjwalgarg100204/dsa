fn main() {}

// should partition the given array sucht that
// last element should be in a place where all
// elements lower than it should be towards its left
// and greater towards its right, in any permutation
fn naive_partition(arr: &mut [i32]) {
    let arr_clone = arr.clone();
}
